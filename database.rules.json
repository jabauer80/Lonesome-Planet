{
  "rules": {
    "users": {
      "$userId": {
        ".read": "auth != null && auth.uid == $userId",
        ".write": "auth != null && auth.uid == $userId"
      }
    },
    "roadmaps": {
      ".read": "auth != null",
      ".write": "auth != null"
    },
    "allocations": {
      ".read": "auth != null",
      ".write": "auth != null"
    }
  }
}